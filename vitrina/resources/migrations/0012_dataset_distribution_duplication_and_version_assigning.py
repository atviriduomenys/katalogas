# Generated by Django 4.2.26 on 2025-12-22 11:18

from django.db import migrations


def duplicate_distributions_and_assign_version(apps, schema_editor):
    DatasetDistribution = apps.get_model("vitrina_resources", "DatasetDistribution")
    Dataset = apps.get_model("vitrina_datasets", "Dataset")
    for dataset in Dataset.objects.all():
        if dataset.dataset_version.exists():
            if distributions_with_no_version := dataset.datasetdistribution_set.filter(metadata_version__isnull=True):
                for distribution_with_no_version in distributions_with_no_version:
                    for version in dataset.dataset_version.all():
                        distribution_with_no_version.pk = None
                        distribution_with_no_version.metadata_version = version
                        distribution_with_no_version.save()
                    distribution_with_no_version.delete()


class Migration(migrations.Migration):

    dependencies = [
        ('vitrina_resources', '0011_datasetdistribution_metadata_version'),
    ]

    operations = [
        migrations.RunPython(duplicate_distributions_and_assign_version, migrations.RunPython.noop)
    ]